*.jl.cov
*.jl.mem
/Manifest.toml
# the examples/ Manifest is committed: PolyStep is dev'd by relative path
# there, so a fresh clone can instantiate and run the examples
lcov.info
/docs/Manifest.toml
/docs/build/
/docs/src/index.md
